ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional -- leave blank or omit to skip Telegram entirely. Without these,
# the bot prints trade/error alerts to the console (captured in cycle.log
# when run via run_cycle.ps1) instead of sending them to Telegram.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
